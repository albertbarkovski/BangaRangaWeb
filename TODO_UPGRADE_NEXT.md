# Next upgrade: amsg.lt-like look

## Steps

1. Redesign `index.html` section structure and markup (hero, benefits, services, projects, CTA, contact, footer).
2. Rework `styles.css` to match modern clean layout (typography, spacing, gradients, card styles, buttons).
3. Keep existing `script.js` behaviors; ensure new sections still use `.section` for reveal.
4. Manual verification in browser.
